export type teleportType = {
  id: number;
  filename: string;
  name: string;
  x: number;
  y: number;
  z: number;
};

export type TeleportFileType = {
  filename: string;
  id: number;
  name: string;
  filename: string;
};

export type TEventAutoStart = {
  type;
};
